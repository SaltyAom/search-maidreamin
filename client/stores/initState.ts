import IInitState from "stores/types/initState"

const initState: IInitState = {
    filter: {
        isOpen: false,
        sortBy: "group",
        orderBy: "ascending"
    }
}

export default initState